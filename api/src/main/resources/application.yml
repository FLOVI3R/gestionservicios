spring:
  datasource:
    url: jdbc:mysql://localhost:3306/gestionservicios
    username: root
    password:
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format-sql: true
  security:
    user:
      name: admin
      password: admin
      